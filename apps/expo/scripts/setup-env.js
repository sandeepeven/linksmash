#!/usr/bin/env node

/**
 * Environment Setup Script
 *
 * This script is no longer needed as the app now uses client-side HTML parsing
 * instead of a backend API. This file is kept for backward compatibility
 * but does not perform any setup operations.
 */

console.log("ℹ️  This script is no longer needed.");
console.log("   The app now uses client-side HTML parsing and does not require API configuration.");
console.log("   You can safely ignore this script.\n");

